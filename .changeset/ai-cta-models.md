---
"vocs": patch
---

Added `aiCta.models` to choose which AI providers (`chatgpt`, `claude`, `gemini`) appear in the CTA dropdown. The first entry becomes the primary button and the rest appear in the dropdown. Omit to show all three with ChatGPT as primary.
