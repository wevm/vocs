---
"vocs": minor
---

Preload the bundled Geist font in the rendered head, add `fontPreload` for custom font hints or opting out, and make default content/gutter geometry independent of font metrics. Font display behavior remains controlled by CSS.
