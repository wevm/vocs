---
"vocs": patch
---

Keep syntax grammars and build-time Markdown/Twoslash plugins out of the configuration loaded by React components, including development updates. Preserve the full configuration for server and build consumers.
