---
"vocs": patch
---

Added an experimental inline Twoslash cache (`twoslash.inlineCache`) that persists results in the markdown source as `// @twoslash-cache: ...` comments so the cache travels with the repo.
