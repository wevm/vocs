---
title: Cadent API — Overview override
---

import { OpenApi } from 'vocs'

# Cadent API

This intro is a **consumer-authored override page** mounted at `/api`. It
replaces the auto-generated spec title/description. The domain list below is
opt-in — rendered via the `<OpenApi.Endpoints />` component.

<OpenApi.Endpoints />

## RPC endpoints

A flat list of all endpoints for the `rpc` resource, via
`<OpenApi.Endpoints resource="rpc" />`:

<OpenApi.Endpoints resource="rpc" />
